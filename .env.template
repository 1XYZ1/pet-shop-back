
STAGE=dev

DB_PASSWORD=MySecr3tPassWord@as2
DB_NAME=TesloDB
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres

PORT=3000
HOST_API=http://localhost:3000/api

# JWT Secret - IMPORTANTE: Generar secret fuerte para producción
# Comando para generar un secret seguro: openssl rand -base64 64
# Este secret de ejemplo solo es para desarrollo local
# NUNCA uses este secret en producción
JWT_SECRET=Est3EsMISE3Dsecreto32s

# CORS Origins - Lista de orígenes permitidos separados por coma
# Desarrollo: frontend local en puerto 5173
# Producción: Reemplazar con tu dominio (ej: https://tudominio.com,https://www.tudominio.com)
CORS_ORIGINS=http://localhost:5173