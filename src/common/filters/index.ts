/**
 * Barrel export para filtros comunes
 *
 * Centraliza las exportaciones de todos los filtros compartidos
 * para facilitar imports en módulos
 *
 * Uso:
 * ```typescript
 * import { HttpExceptionFilter } from '@/common/filters';
 * ```
 */

export { HttpExceptionFilter } from './http-exception.filter';
